export const enum TezosAccountConnectionStatus {
  NotConnected = 0,
  Connected = 10
}

export const enum EtherlinkAccountConnectionStatus {
  NotConnected = 0,
  AddNetwork = 1,
  SwitchNetwork = 2,
  Connected = 10
}
